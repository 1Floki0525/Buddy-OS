#!/usr/bin/env bash
# Buddy-OS broker daemon entrypoint inside the buddy-core snap

export BUDDY_POLICY_PATH="${BUDDY_POLICY_PATH:-$SNAP/etc/buddy/policy.json}"

exec /usr/bin/python3 "$SNAP/lib/broker/buddy_actionsd.py"
