# Model Assertion Notes

- Replace `<YOUR_BRAND_ID>` with the brand/authority ID tied to your snapcraft account.
- `classic: true` enables classic mode on Ubuntu Core.
- `buddy-voice` is a planned snap for Whisper.cpp + VAD + Piper voices.
- Update the `timestamp` when issuing the signed model assertion.
